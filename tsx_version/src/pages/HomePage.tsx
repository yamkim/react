import React from "react";
import tw, { css } from "twin.macro";

const HomePage = () => {
    return (
        <>
            <button
                className="btn-base"
            > Hello</button>
            <button
                className="btn-base"
            > World</button>
        </>
    );
};

export default HomePage;